---
import { site, nav } from '@/content/site';

const current = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<header class="border-b border-oat/60 bg-cream/80 backdrop-blur">
  <div class="container-wide flex items-center justify-between py-4">
    <a href="/" class="flex items-center gap-2 font-display text-xl font-semibold text-ink" aria-label={`${site.name} home`}>
      <img src="/logo.svg" alt="" width="32" height="32" class="h-8 w-8" aria-hidden="true" />
      <span>{site.name}</span>
    </a>
    <nav aria-label="Primary">
      <ul class="flex items-center gap-1 sm:gap-2">
        {nav.map((item) => {
          const active = (item.href === '/' && current === '/') || (item.href !== '/' && current.startsWith(item.href));
          return (
            <li>
              <a
                href={item.href}
                aria-current={active ? 'page' : undefined}
                class:list={[
                  'rounded-full px-3 py-2 text-sm font-medium transition sm:px-4',
                  active ? 'bg-oat text-ink' : 'text-ink/80 hover:bg-oat/60 hover:text-ink',
                ]}
              >
                {item.label}
              </a>
            </li>
          );
        })}
      </ul>
    </nav>
  </div>
</header>
